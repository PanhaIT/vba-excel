 'insert inventory valucation
                            inventory.Cells(invValId, 1)    = invValId 'id
                            inventory.Cells(invValId, 2)    = 1 'company_id
                            inventory.Cells(invValId, 3)    = branchId 'branch_id
                            inventory.Cells(invValId, 4)    = "" 'inv_adj_id
                            inventory.Cells(invValId, 5)    = "" 'sales_invoice_id
                            inventory.Cells(invValId, 6)    = "" 'sale_return_id
                            inventory.Cells(invValId, 7)    = "" 'purchase_bill_id
                            inventory.Cells(invValId, 8)    = "" 'purchase_bill_detail_id
                            inventory.Cells(invValId, 9)    = "" 'purchase_return_id
                            inventory.Cells(invValId, 10)   = "" 'point_of_sales_id
                            inventory.Cells(invValId, 11)   = "" 'purchase_receive_result_id
                            inventory.Cells(invValId, 12)   = "" 'purchase_receive_id
                            inventory.Cells(invValId, 13)   = "Invoice" 'type
                            inventory.Cells(invValId, 14)   = invoiceCode 'reference
                            inventory.Cells(invValId, 15)   = customerId 'customer_id
                            inventory.Cells(invValId, 16)   = "" 'vendor_id
                            inventory.Cells(invValId, 17)   = invoiceDate 'date
                            inventory.Cells(invValId, 18)   = productId 'product_id
                            inventory.Cells(invValId, 19)   = qtyOrderSmall 'small_qty
                            inventory.Cells(invValId, 20)   = qtyOrder'qty
                            inventory.Cells(invValId, 21)   = 0 'cost
                            inventory.Cells(invValId, 22)   = 0 'price
                            inventory.Cells(invValId, 23)   = 0 'on_hand
                            inventory.Cells(invValId, 24)   = 0 'on_hand_small
                            inventory.Cells(invValId, 25)   = 0 'avg_cost
                            inventory.Cells(invValId, 26)   = 0 'asset_value
                            inventory.Cells(invValId, 27)   = createdDate 'created
                            inventory.Cells(invValId, 28)   = "" 'date_edited
                            inventory.Cells(invValId, 29)   = 0 'is_refer_gm_id
                            inventory.Cells(invValId, 30)   = 0 'avg_refer
                            inventory.Cells(invValId, 31)   = 1 'is_var_cost
                            inventory.Cells(invValId, 32)   = 0 'is_adjust_value
                            inventory.Cells(invValId, 33)   = 1 'is_active
                            invValId = invValId + 1